{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "yarn --cwd backend install && yarn --cwd frontend install --production=false && yarn --cwd frontend build",
    "start": "yarn --cwd backend start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
